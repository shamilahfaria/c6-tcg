pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r tcg public/tcg
    - cp index.html .nojekyll public/ 2>/dev/null || true
    - 'echo "<!doctype html><meta http-equiv=refresh content=\"0;url=tcg/\">" > public/index.html'
  artifacts:
    paths: [public]
  pages: true
